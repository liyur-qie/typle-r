# v0.6.0 — shadcn/uiへの移行

UIをshadcn/ui（Radix / Nova）とTailwind CSS 4で再実装しました。

- ボタン、入力欄、ラベル、テーブル、カード、通知、バッジ、読み込み表示を共通コンポーネントに統一。
- ナビゲーションと画面全体を共通のテーマ・フォーカス表示に統一。
- 削除確認をAlertDialogへ移行。キャンセルと、保存失敗後のダイアログ内再試行に対応。
- 公式CLI用のcomponents.json、テーマ、コンポーネントソースを追加。

既存のURL、GitHub認証、IME入力、単語リスト・練習記録の保存形式を維持しています。DBマイグレーションは不要です。

## ローカル検証

- 本番ビルド、型チェック、Lintが通過。
- 単体・実DBテスト10件、Playwright E2E 9件が通過（workers上限10）。
- 削除キャンセル、削除失敗からの再試行、アカウント切り替え、IME入力を確認。
- 390px・1440px幅の表示とキーボード操作を確認。
- npm auditの指摘は0件。

GitHub CIとVercelデプロイは停止を維持しています。
