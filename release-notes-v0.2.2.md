# v0.2.2 — Tailwind CSSへの統一

MUIを廃止し、ボタン・入力欄・ナビゲーションを標準HTMLとTailwind CSSで実装しました。

- MUI、Emotion、Sass、Roboto配信依存とSCSSを削除。
- ボタンの通常・枠線・塗りつぶし・削除・無効状態、入力ラベル・必須検証・キーボードフォーカスを維持。
- モバイルでは上部ナビゲーションに切り替え、現在のページを表示。
- 作成画面の初回JavaScriptをビルド表示で約151kBから95.4kBに削減。

ローカル検証: 型チェック、単体8件、Lint、本番ビルド、Docker PostgreSQLを利用するPlaywright E2E8件が成功。390pxと1440pxの画面表示、横スクロールの有無、キーボード操作も確認しました。

GitHub CIは再許可まで無効のままです。Vercelデプロイも停止中です。認証・保存仕様と既存の依存関係に関する制約は継続します。
