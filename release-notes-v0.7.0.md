# v0.7.0 — 練習画面と単語編集UIの改善

- 全画面に日本語・英語対応のNoto Sans JPを適用。
- 練習画面の単語表示と入力欄をカード幅いっぱいに揃え、高さ・文字サイズ・中央配置を統一。
- 表示文字を入力と1文字ずつ比較し、正解は緑、不正解は赤に色分け。重複テキストや入力エラーの枠・メッセージを整理。
- 練習中は単語をバッジ表示し、入力済みの単語を緑色に表示。
- 完了時はリスト名入りの見出しと結果テーブルを表示。リスト選択も2列テーブルに変更。
- 作成・編集フォームをレスポンシブなテーブルに変更。入力・補足はボタングループで開閉し、並べ替え・削除はアイコンボタンに統一。
- 入力文字は標準で表示単語に連動し、異なる入力文字も個別に指定可能。
- トップバーの高さとログアウトボタンのサイズを調整。

## 検証と運用

本番ビルド（型チェックを含む）を確認しました。ユーザー指示により、今回のリリースではLint・E2Eは実行していません。E2Eの定義はUI変更に合わせて更新していますが、最終状態での実行検証は未実施です。

GitHub CIとVercelデプロイは停止を維持しています。DBスキーマ・保存形式の変更はなく、マイグレーションは不要です。
